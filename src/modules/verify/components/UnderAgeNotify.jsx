import React from 'react'

const UnderAgeNotify = ({ display }) => (
    <h2 
    style={{ display }}
    className="verify-header"
    >
        Sorry, you must be 21 or older to view this site
    </h2>
)

export default UnderAgeNotify